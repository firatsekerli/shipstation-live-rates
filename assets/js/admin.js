jQuery(document).ready(function($) {
    'use strict';

    console.log('ShipStation: Admin JS loaded');

    // All service selection is now handled via server-side checkbox table
    // No AJAX loading needed anymore
});
